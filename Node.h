#ifndef NODE_H
#define NODE_H

// Structure for one tree node
struct Node {

    int data;        // value stored
    Node* left;      // left child pointer
    Node* right;     // right child pointer
};

#endif
